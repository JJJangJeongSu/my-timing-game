import 'package:flutter/material.dart';

class ScoreViewer extends StatelessWidget {
  const ScoreViewer({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Text("3", style: TextStyle(
      fontSize: 30
    ),);
  }
}